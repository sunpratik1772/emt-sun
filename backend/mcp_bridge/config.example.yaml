# Example MCP server definitions (Hermes Agent + OpenClaw compatible patterns).
# Studio uses MCP_BRIDGE_URL → this HTTP bridge instead of spawning stdio servers.

# --- Hermes (~/.hermes/config.yaml) ---
# mcp_servers:
#   atlassian:
#     command: npx
#     args: ["-y", "@modelcontextprotocol/server-atlassian"]  # or mcp-atlassian via uvx
#     env:
#       ATLASSIAN_SITE_URL: https://your-company.atlassian.net
#       ATLASSIAN_EMAIL: you@company.com
#       ATLASSIAN_API_TOKEN: "***"
#   github:
#     command: npx
#     args: ["-y", "@modelcontextprotocol/server-github"]
#     env:
#       GITHUB_PERSONAL_ACCESS_TOKEN: "***"

# --- OpenClaw (~/.openclaw/openclaw.json) ---
# {
#   "mcp": {
#     "servers": {
#       "atlassian": {
#         "command": "npx",
#         "args": ["-y", "@sooperset/mcp-atlassian"]
#       },
#       "github": {
#         "command": "npx",
#         "args": ["-y", "@modelcontextprotocol/server-github"],
#         "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}" }
#       }
#     }
#   }
# }

# --- Studio (env for backend + workflows) ---
# MCP_SERVER_URL=http://127.0.0.1:8765
# MCP_BRIDGE_MODE=demo   # demo | live (live passthrough: future)
